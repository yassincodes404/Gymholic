package com.gymholic.common.enums;

/**
 * The bookable service types. The three paid services are 45-minute
 * consultations; the free time session is a complimentary 3-hour block,
 * limited to one per trainer per expert-local day.
 */
public enum BookingServiceType {
    STRATEGY_CALL,
    IN_PERSON,
    OPEN_SESSION,
    FREE_SESSION
}
